apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: flag-service
spec:
  podSelector:
    matchLabels:
      app: flag-service
  policyTypes:
    - Ingress
  ingress:
    - from:
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: ingress-nginx
      - namespaceSelector:
          matchLabels:
            kubernetes.io/metadata.name: togglemaster-evaluation
      ports:
        - protocol: TCP
          port: 8002
